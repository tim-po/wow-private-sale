import React from 'react';
import './index.scss';

type FedorProps = {
// Declare your props here
};

export default function Fedor(props: FedorProps) {
return (
<div className="fedor">
  {/* Add the content or children you want to render here */}
</div>
);
}
